A simple application that displays a list of movies fetched from a local JSON file. 
This project follows the MVVM (Model-View-ViewModel) architecture to ensure a clean separation of concerns, maintainability, and scalability.
